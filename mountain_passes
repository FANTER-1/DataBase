CREATE TABLE mountain_passes (
    id SERIAL PRIMARY KEY,
    coordinates POINT,
    height INTEGER,
    name VARCHAR(255),
    photos TEXT[],
    user_name VARCHAR(255),
    email VARCHAR(255),
    phone VARCHAR(20)
);
